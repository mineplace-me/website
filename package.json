{
  "name": "mineplace-monorepo",
  "private": true,
  "engines": {
    "node": ">=23.11.0"
  },
  "scripts": {
    "build": "cd packages/bluemap && bun run build && cd ../mineplace && bun run build",
    "start": "cd packages/bluemap && bun run build && cd ../mineplace && bun run start",
    "serve": "cd packages/bluemap && bun run build && cd ../mineplace && bun run build && bun serve"
  },
  "type": "module",
  "devDependencies": {
    "@nx/js": "^21.4.0",
    "nx": "21.4.0",
    "tsx": "^4.20.4",
    "yargs": "^18.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "trustedDependencies": [
    "@parcel/watcher",
    "@tailwindcss/oxide"
  ]
}