{
  "name": "mineplace-monorepo",
  "private": true,
  "scripts": {
    "build": "cd packages/bluemap && bun run build && cd ../mineplace && bun run build",
    "start": "cd packages/bluemap && bun run build && cd ../mineplace && bun run start",
    "serve": "cd packages/mineplace && bun serve"
  },
  "type": "module",
  "devDependencies": {
    "@nx/js": "^22.3.3",
    "nx": "22.3.3",
    "tsx": "^4.21.0",
    "yargs": "^18.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "trustedDependencies": [
    "@parcel/watcher",
    "@tailwindcss/oxide"
  ]
}